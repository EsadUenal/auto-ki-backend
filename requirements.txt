# Auto-KI Backend — Phase 1
# Installieren: python -m pip install -r requirements.txt

# API-Framework
fastapi[standard]>=0.136
uvicorn>=0.48

# Rate-Limiting
slowapi>=0.1.9

# Daten-Validierung
pydantic>=2.13

# Vektor-Datenbank (Fließtext, Embedding lokal via ONNX)
chromadb>=1.5

# LLM-Anbindung (Google Gemini)
google-genai>=2.7

# HTTP-Client (Tavily Search API + async Requests)
httpx>=0.27

# Auth — Phase 2b
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Zahlungen — Phase 2d
stripe>=7.0.0

# Umgebungsvariablen aus .env laden
python-dotenv>=1.0.0
