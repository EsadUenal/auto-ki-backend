# Vira Backend — Produktions-Abhängigkeiten (gepinnt für reproduzierbare Builds)
# Installieren: python -m pip install -r requirements.txt
#
# Versionen exakt auf den getesteten Stand gepinnt. Vor einem Upgrade eines
# Pakets: lokal testen (python test_*.py), dann Pin hier anheben. Ungepinnte
# ">="-Constraints führten sonst dazu, dass ein Prod-Build eine ungetestete
# (evtl. brechende) Upstream-Version zieht.

# API-Framework
fastapi[standard]==0.136.3
uvicorn==0.48.0

# Rate-Limiting
slowapi==0.1.9

# Daten-Validierung
pydantic==2.13.4

# Vektor-Datenbank (Fließtext, Embedding lokal via ONNX)
chromadb==1.5.9

# LLM-Anbindung (Google Gemini)
google-genai==2.7.0

# HTTP-Client (Tavily Search API + async Requests)
httpx==0.28.1

# Auth
passlib[bcrypt]==1.7.4
bcrypt==5.0.0            # explizit gepinnt: passlib zieht bcrypt sonst ungepinnt
python-jose[cryptography]==3.5.0

# Zahlungen (Stripe)
stripe==15.2.1

# Umgebungsvariablen aus .env laden
python-dotenv==1.0.1
